# .gitignore

# logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# dependencies / build outputs
node_modules/
dist/
dist-ssr/
build/
.vite/

# envs
*.local
.env
.env.*   # e.g., .env.development, .env.production

# IDE
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# tests -------------- (CHANGED)
coverage/
.nyc_output/              # added
# DO NOT ignore test sources; keep them in repo:
# (remove these lines that were ignoring tests)
# *.test.ts
# *.spec.ts

# docs --------------- (CHANGED)
# Keep portfolio assets; ignore only raw/working sources
# (remove previous: 'docs/' + whitelists)
docs/raw/**               # e.g., original .psd/.ai/.mp4 sources
docs/tmp/**
docs/**/*.psd
docs/**/*.ai
docs/**/*.sketch
# keep images/GIF/markdown under docs/
# e.g., docs/img/**, docs/gif/**, docs/*.md will be tracked

# etc
.cache/
.eslintcache              # added
*.tgz

# backend build outputs (keep repo clean)
server/bin/
server/obj/
bin/
obj/

# Visual Studio
.vs/
*.user                    # added
*.rsuser                  # added

# Rider / Resharper
_ReSharper.Caches/

# EF Core Migrations Snapshots (optional)
*.dbmdl

# Local DB files (optional but recommended)
*.db
*.sqlite
*.mdf
*.ldf

# Windows junk
Thumbs.db
desktop.ini              # added

# --- Custom Project Rules ---

# Sample data files
import_sample_*.json
sample_uploads/

# Personal devlogs
TraceNet*.md
*.md

# Postman collections/envs (only ignore personal ones)
*.postman_collection.json
postman/*.postman_environment.json           # added
!postman/official_*.postman_collection.json
!postman/official_*.postman_environment.json # added

# etc.
**/bin/
**/obj/
**/*.pdb
